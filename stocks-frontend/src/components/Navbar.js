import React from 'react';
import './Navbar.css';

const Navbar = () => {
    return (
        <header>
            <nav className="navbar">
                <div className="navbar-brand">
                    <a href="/">Stock Management App</a>
                </div>
            </nav>
        </header>
    );
};

export default Navbar;
